spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/Redis_Practice?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456789
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
      format_sql: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PW:1234}

jwt:
  secret: "P@ssw0rd!SecureJWTSecretKey1234567890"
  expiration: 3600000